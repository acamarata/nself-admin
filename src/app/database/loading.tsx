// Loading state for Database page - shown while the page JavaScript loads
export default function Loading() {
  // Return null for instant loading - the page will handle its own loading state
  return null
}